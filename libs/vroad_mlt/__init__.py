"""vroad_mlt — librerías comunes de VisualRoad ML Training.

Paquete con la lógica compartida por los jobs/servicios/workflows del repo,
para no repetir código (formato de datos, naming, GCS/BigQuery, locks, métrica,
manifiesto y contrato del modelo). Ver CLAUDE.md.
"""

__version__ = "0.0.1"
